#include <iostream>

using namespace std;

int main()
{
    int c=1000,d=1,x=0,y=0;
    while (d<=c)
    {
        if (d%3==0)
        {
            x+=d;
        }
        if (d%5==0)
        {
            y+=d;
        }
        d++;
    }
    cout<<x+y;

}
